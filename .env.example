# Helius API Key for Devnet RPC
# Get your free API key at: https://www.helius.dev/
HELIUS_API_KEY=your_helius_api_key_here

# NOTE: Currently Anchor.toml (line 32), package.json (lines 10, 19-20),
# and tests/devnet-bundle.ts (line 22) still contain the API key hardcoded.
# Anchor.toml doesn't support env vars natively.
#
# For production:
# 1. Rotate your API key at https://www.helius.dev/
# 2. Update all occurrences manually
# 3. Keep .env file gitignored (already in .gitignore)
